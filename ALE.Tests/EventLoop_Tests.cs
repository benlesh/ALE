using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Threading;

namespace ALE.Tests
{
	[TestClass]
	public class EventLoop_Tests
	{
		[TestMethod]
		public void EventLoop_Pend_Next()
		{
			//var loop = new EventLoop();
			//var list = new List<int>();
			//loop.Pend(() => list.Add(2));
			//loop.Pend(() => list.Add(1));
			//loop.Pend(() => list.Add(3));
			//loop.StartEventLoop();
			//Thread.Sleep(3000);
			//loop.Stop();
			//Assert.AreEqual(3, list.Count);
		}
	}
}
